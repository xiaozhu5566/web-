
1. 高级 React-Router React-Redux

2. classList.remove/add()

transform: translateY(-50%);  向上移动自己的50%

react引入图片 用import，直接引入-找不到

class - className

jsx中  < 会被识别为一个标签的开始。 --> {'>'} / &lt;

react中相似的东西 一般用 map 数组

map渲染的列表中必须有一个唯一不相等的key，（能保持react良好的性能） 通过对比key的不同，react进行选择更新  -- diff算法
return后的括号

思想的过度才是最重要的！！！ React是用数据，js生成HTML的。如果你发现自己重复的写HTML，那是你没有觉悟react的思想。


****
1. map与forEach区别

2. items.forEach is not a function;
var items = document.getElementsByClassName('item')其不能用forEach遍历吗？